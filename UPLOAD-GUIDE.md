# 황성은 · Mobile Engineer Portfolio

GitHub Pages에서 동작하는 정적 포트폴리오입니다. HTML, CSS, JavaScript만 사용하므로 npm 패키지 설치나 서버가 필요하지 않습니다.

## 바로 보기

`index.html`을 브라우저에서 열거나, 프로젝트 폴더에서 `python3 -m http.server 4173`을 실행한 뒤 `http://localhost:4173`에 접속합니다.

## GitHub Pages에 올리기

### 가장 간단한 방법

1. GitHub에 `hwangsungeun.github.io` 저장소를 만듭니다. 이미 같은 저장소가 있으면 기존 내용을 먼저 확인하세요.
2. 제공된 `portfolio-github-pages.zip`을 풀고 **압축 안의 파일과 assets 폴더**를 저장소의 최상위에 업로드합니다. 최상위에 `index.html`이 있어야 합니다. `원본` 폴더는 올릴 필요가 없습니다.
3. 저장소의 **Settings → Pages → Build and deployment → Source**에서 **Deploy from a branch**를 선택합니다.
4. **main / (root)**를 선택하고 Save를 누릅니다.
5. 배포가 완료되면 `https://hwangsungeun.github.io/`에서 확인합니다.

별도 프로젝트 저장소 이름을 사용해도 됩니다. 이때 주소는 `https://hwangsungeun.github.io/저장소이름/`입니다. 모든 사이트 내부 파일 경로를 상대 경로로 작성해 두 형식을 지원합니다. GitHub 아이디는 원자료의 `hwangsungeun`을 기준으로 작성했습니다.

[GitHub 공식 Pages 생성 안내](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site)

### 수정 후 다시 묶기

Node.js가 설치되어 있으면 `npm run build`를 실행합니다. 별도 `npm install`은 필요 없습니다. 배포용 파일이 `dist/`에 생성됩니다. `dist/` 안의 내용을 업로드하세요.

`npm run check`는 JavaScript 문법을 검사합니다. 빌드 과정은 프로젝트 ID, 내부 링크, 이미지 파일과 대체 텍스트를 확인합니다.

## 수정 위치

- `index.html`: 메인 화면 문구, 프로젝트 카드, 경력, 연락처
- `data.js`: 프로젝트 상세 내용·이미지·기술 태그
- `styles.css`: 색상, 타이포그래피, 배치, 모바일 화면
- `app.js`: 상세 창, URL 연결, 이메일 복사, 현재 섹션 표시
- `assets/`: 원본에서 추출한 앱 이미지와 제공받은 최신 이력서 PDF

프로젝트 제목·설명을 바꾸면 `index.html` 카드와 `data.js` 상세를 함께 수정하세요. 특정 프로젝트는 `#project-gurufin`처럼 직접 연결할 수 있습니다. 페이지에 표시된 앱 화면을 누르면 상세를 볼 수 있고 상세 이미지 자체를 누르면 큰 이미지를 새 탭에서 엽니다. Esc, 닫기 버튼, 배경 클릭, 브라우저 뒤로가기로 상세 창을 닫을 수 있습니다.

Google Fonts가 연결되지 않아도 시스템 글꼴로 표시됩니다. 배포 파일에는 별도 분석 도구나 백엔드 연결이 없습니다. 이력서 PDF 및 연락처는 사용자가 제공한 최신 자료를 그대로 사용했습니다.

## 반영한 자료와 기준

- `01_Original_Portfolio.pptx`: 40장 프로젝트 기능 및 실제 앱 이미지
- `02_Latest_Portfolio.html`: 최신 Gurufin 사례, 지갑·게임 화면, 기술 및 성과
- `03_Latest_Resume.pdf`: 2026.08 경력·기간·직책의 우선 기준

일부 초기 프로젝트 기간은 PPT와 이력서가 다릅니다. 사이트의 기간은 최신 이력서에 맞췄습니다. 테스트 175개는 **테스트 파일 수**, 본인 추가분은 **171개**입니다. R8 resources.txt 78.16%는 **해당 산출물 크기 감소율**이며 앱 용량·실행 속도 개선율이 아닙니다. 오류 0건은 **해당 릴리스 업데이트·콜드 스타트 검증 범위**입니다. 원자료의 수치를 반영했으며 실제 업무 저장소를 재검증한 결과는 아닙니다.

## 디자인 참고

- [Brittany Chiang](https://brittanychiang.com/): 경력과 프로젝트를 빠르게 읽는 정보 구성
- [Rauno Freiberg](https://rauno.me/): 개인의 작업을 탐색하며 읽는 경험

참고 사이트의 코드·문구·이미지는 복사하지 않았습니다. 구성과 스타일을 새로 작성했고, 앱 이미지는 제공된 원본 자료에서 추출했습니다.


## 이번 개편

- 첫 화면의 마인드힐 이미지를 한스타일·빗썸라이브로 교체했습니다. Gurufin Wallet과 함께 3개 제품을 보여줍니다.
- 대표 지갑과 9개 프로젝트를 모두 본문에 노출하고 13개 회사 경력도 펼쳤습니다.
- 모든 이미지는 원본 비율을 유지하며, 독립된 화면 영역으로 감싼 뒤 프레임 전체에만 회전을 적용합니다. 화면 이미지 자체를 임의로 늘리거나 잘라 맞추지 않습니다.
- 첫 화면 부유 효과, 데스크톱 포인터 반응, 스크롤 등장 효과, 흐르는 기술 리본, 카드 호버와 상세 창 전환을 적용했습니다.
- 상단의 모션 끄기로 반복 애니메이션을 정지할 수 있습니다. 기기의 동작 줄이기 설정도 존중합니다.
- 320·375·390·768·1024·1440·1920px 브라우저 뷰포트에서 가로 넘침, 카드 잘림, 이미지와 프레임 일치를 검사했습니다. 실제 모바일 기기 테스트와는 구분됩니다.
